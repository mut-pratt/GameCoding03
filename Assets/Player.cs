using UnityEngine;

public class Player: MonoBehaviour {
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public Potion CurrentUsing;

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
